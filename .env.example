LINE_CHANNEL_SECRET=your_line_channel_secret
LINE_CHANNEL_ACCESS_TOKEN=your_line_access_token
TARGET_DRIVE_FOLDER_ID=your_gdrive_folder_id
# Path to the service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=credentials.json
PORT=8000
# (Optional) Set to true to automatically start ngrok and update LINE webhook in local dev
USE_NGROK=true
# (Optional) ngrok authtoken for persistent tunnels (REQUIRED for automatic mode)
NGROK_AUTHTOKEN=your_ngrok_authtoken
